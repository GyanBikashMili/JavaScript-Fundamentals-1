//You are creating a countdown app. Implement a program that uses a while loop to count down from 10 to 1
// and prints each number.
let count = 10;

while (count >= 1) {
  console.log(count);
  count--;
}
